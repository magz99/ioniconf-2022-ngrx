import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ioniconf-ngrx-routing',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
